# Gilded Rose With Strategy Pattern in Java

## Run tests:
`gradle test`

You can activate the legacy code in the GildedRose class and run the tests against it as well. In that case, skip ConjuredUpdateStrategyTest as it tests the new requirement logic which is not implemented in the legacy code.
